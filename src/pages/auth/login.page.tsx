import AuthLayout from "../../layouts/auth.layout"
import FormLoginComponent from "../../components/auth/form-login.component"

export const LoginPage = () => {
  return (
    <AuthLayout>
      <FormLoginComponent />
    </AuthLayout>
  )
}
